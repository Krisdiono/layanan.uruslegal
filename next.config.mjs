/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Kalau sebelumnya kamu pakai standalone untuk Vercel/Node:
  output: 'standalone',
  // Pindahkan opsi lain dari file TS lama ke sini kalau ada.
};
export default nextConfig;
