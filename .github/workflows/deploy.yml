# ...
- name: Install deps
  run: npm ci

- name: Build
  run: npm run build

# 👉 Bundle minimal untuk runtime Next standalone
- name: Prepare deploy bundle (standalone)
  run: |
    rm -rf deploy && mkdir -p deploy/.next
    cp -r .next/standalone deploy/.next/standalone
    cp -r .next/static deploy/.next/static
    [ -d public ] && cp -r public deploy/public || true

- name: Zip bundle
  run: |
    cd deploy
    zip -r ../out.zip .

# login & deploy tetap sama
# ...
